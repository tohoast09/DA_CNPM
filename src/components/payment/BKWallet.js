import * as React from 'react';
import Button from '@mui/material/Button';

export default function BKWallet() {
  return (
    <Button variant="contained" disableElevation size="large">
      Disable elevation
    </Button>
  );
}